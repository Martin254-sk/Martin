//do while loop
//Author:Martin Nzioki Ngao
//Registration NO.CT/101/G/22078/24
#include<stdio.h>
int main(  ) {
    int start,stop,sum=0;
  printf("Enter start:");
  scanf("%d",&start);
  printf ("Enter when to stop:");
  scanf("%d",&stop);
  
  do{printf("%d\n",start);
  sum=start;
  sum+start;
  start++;
  } 
  while ("start,star<=stop");
  printf("the sum%d",sum);
  
  return 0;
}